#include <stdio.h>

int main() // main program function
{
    puts("Hello, World!"); // puts -> function called to output the text to standard output
    // printf("Hello, World!");
    
    return 0;
}