#include <stdio.h>

int main() {
    int age = 25; // var age assigned 25
    printf("Age is %d", age);

    age = 26; // var age value changed
    printf("\nNew age is %d", age); // new age printed with a new line
    
    return 0;
}