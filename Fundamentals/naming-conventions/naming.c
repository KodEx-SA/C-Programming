#include <stdio.h>
#include <stdlib.h>

int main() {
    // variable naming conventions: use descriptive names
    int playerScore = 0;
    int numberOfLives = 3;
    char playerName[50];

    // 1. variable names must start with a letter or an underscore
    int age = 26;
    int _age = 30; // valid but not recommended
    // int 1age = 25; // invalid, cannot start with a digit

    // 2. variable names can contain letters, digits, and underscores
    // 3. variable names should be descriptive and meaningful
    // 4. variable names should follow a consistent naming convention (e.g., camelCase, snake_case) - case sensitive

    return 0;
}